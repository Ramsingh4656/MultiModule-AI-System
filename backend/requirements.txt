# FastAPI and Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.44
alembic==1.12.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.0          # MUST be 4.0.0 — do not change

# AI/ML Libraries
# (Spam detection is implemented in pure Python to avoid native deps.)

# PDF Processing
PyPDF2==3.0.1

# Utilities
python-dotenv==1.0.0
pydantic>=2.12.0
pydantic-settings>=2.11.0
